<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="carpool">

	<!-- 전체조회 -->
	<select id="carpool_list" resultType="vo.CarpoolVo">
		select c.* ,
        (select count(carpool_idx) from CARPOOL_LIKE where c.carpool_idx=carpool_idx) cnt
		from 
  		(select * from CARPOOL order by carpool_idx desc) c 
	</select>

	<!-- 게시글 추천 시 Like 테이블에 insert -->
	<insert id="carpool_insertlike">
	insert into carpool_like
	values(4,28,#{carpool_idx},1)
	</insert>
	
	<!-- 게시글 추천취소 시 좋아요 테이블에서 삭제  -->
	<update id="carpool_deletelike">
	delete from carpool_like 
	where carpool_idx = #{carpool_idx} and mem_idx = #{mem_idx}
	</update>
	

</mapper>












