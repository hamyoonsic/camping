<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="carpool_like">

	<!-- 전체조회 -->
	<select id="carpool_like_list" resultType="vo.CarpoolVo">
		select * from carpool_like where carpool_idx=#{carpool_idx} and mem_idx=#{mem_idx}
	</select>

</mapper>












